import styled from "styled-components";
import { Theme } from "theme/theme";

const Body = styled.p`
  font-size: 14px;
  color: ${Theme.colors.danger};
  margin: 0;
  padding-top: 5px;
`;

export { Body };
